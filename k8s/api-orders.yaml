apiVersion: apps/v1
kind: Deployment
metadata: { name: api-orders, namespace: agent-stack }
spec:
  replicas: 1
  selector: { matchLabels: { app: api-orders } }
  template:
    metadata:
      labels: { app: api-orders }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: api-orders
        image: api-orders:latest
        ports: [ { containerPort: 8000 } ]
        env:
        - { name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "http://otel-collector:4318" }
---
apiVersion: v1
kind: Service
metadata: { name: api-orders, namespace: agent-stack }
spec:
  selector: { app: api-orders }
  ports: [ { port: 80, targetPort: 8000 } ]
